let CART_PRODUCTS=[];
async function renderCart(){const body=document.getElementById('cartBody');if(!body)return;const cart=cartRead();if(!cart.length){body.innerHTML='<div class="empty">Your bag is empty.<br><br><a class="btn" href="products.html">Browse the collection</a></div>';return;}try{CART_PRODUCTS=(await api('/api/products')).products;let subtotal=0;const lines=cart.map(c=>{const p=CART_PRODUCTS.find(x=>x.id===c.id);if(!p)return '';subtotal+=p.price*c.qty;return `<div class="cart-line"><a href="product.html?id=${p.id}"><img src="${productImg(p)}" alt="${escapeHtml(p.name)}"></a><div><div class="name">${escapeHtml(p.name)}</div><div class="sub">${escapeHtml(p.category)} · ${fmtINR(p.price)} each</div><div class="sub">Size: ${escapeHtml(c.size||p.sizes[0])}</div></div><div class="qty"><button onclick="changeQty(${p.id},-1)">−</button><span>${c.qty}</span><button onclick="changeQty(${p.id},1)" ${c.qty>=p.stock?'disabled':''}>+</button></div><div style="font-weight:700">${fmtINR(p.price*c.qty)}</div><button class="remove" onclick="removeFromCart(${p.id},'${escapeHtml(c.size||'')}')">Remove</button></div>`}).join('');const shipping=subtotal>=FREE_SHIPPING_MIN?0:SHIPPING_FEE,total=subtotal+shipping;const logged=!!localStorage.getItem('sh_token');body.innerHTML=lines+`<div class="cart-summary"><div class="row"><span>Subtotal</span><span>${fmtINR(subtotal)}</span></div><div class="row"><span>Delivery</span><span>${shipping?fmtINR(shipping):'Free'}</span></div><div class="row total"><span>Total</span><span>${fmtINR(total)}</span></div><p class="policy-callout"><strong>Order policy:</strong> Orders are non-refundable. If an item arrives damaged, defective or incorrect, contact Style Heaven support to raise an eligible claim.</p>${logged?'<a class="btn blush" style="width:100%;margin-top:16px;text-align:center" href="checkout.html">Checkout</a>':'<a class="btn blush" style="width:100%;margin-top:16px;text-align:center" href="login.html?redirect=checkout.html">Log in to checkout</a>'}</div>`}catch(e){body.innerHTML=`<div class="empty">${escapeHtml(e.message)}</div>`}}
function changeQty(id,delta){const p=CART_PRODUCTS.find(x=>x.id===id);let cart=cartRead();const line=cart.find(c=>c.id===id);if(!line)return;line.qty=Math.min(Math.max(0,line.qty+delta),p?.stock||line.qty+delta);if(line.qty<=0)cart=cart.filter(c=>c!==line);cartWrite(cart);renderCart()}
function removeFromCart(id,size){let cart=cartRead().filter(c=>!(c.id===id&&(!size||c.size===size)));cartWrite(cart);renderCart()}
document.addEventListener('DOMContentLoaded',renderCart);
